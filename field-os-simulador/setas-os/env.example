# Cuenta de prueba de Firebase (proyecto sdlp-os) usada por Playwright para
# pasar el login (#setas-auth-gate) antes de correr los E2E.
# Créala en: console.firebase.google.com/project/sdlp-os/authentication/users
# Copia este archivo a .env (ya ignorado por git) y complétalo.
E2E_TEST_EMAIL=
E2E_TEST_PASSWORD=
